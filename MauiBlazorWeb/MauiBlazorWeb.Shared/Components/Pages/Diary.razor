@page "/diary"
@using Interfaces

@inject IAppState appState

@code {
    protected override async Task OnInitializedAsync()
    {
        appState.Title = "Diary";
        await InvokeAsync(StateHasChanged);
    }
}
